name: risk.test
description: A sample Polymer application
dependencies:
  morph: ">=0.8.2 <0.9.0"
  risk_engine:
    path: ../risk_engine
  risk:
    # Change here target directory if you start from a new step
    path: ../samples/s1_basics
dev_dependencies:
  unittest: any
  mock: any
